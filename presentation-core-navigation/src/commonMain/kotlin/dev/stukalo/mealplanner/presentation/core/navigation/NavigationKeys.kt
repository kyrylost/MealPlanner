package dev.stukalo.mealplanner.presentation.core.navigation

/**
 * Common keys used for passing data between screens via Navigation.
 */
object NavigationKeys {
    /** Key for passing filter results back to the search screen. */
    const val FILTER_RESULT = "filter_result"
}
